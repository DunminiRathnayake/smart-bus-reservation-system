/**
 * @file paymentGateway.js
 * @description Payment gateway constants for the SmartGo system.
 */

const PaymentGateway = {
  SIMULATED: 'SIMULATED'
};

// Freeze the object to prevent runtime modification
Object.freeze(PaymentGateway);

module.exports = PaymentGateway;
